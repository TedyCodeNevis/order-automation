from config import settings 

print(f"Token bot is {settings.BOT_TOKEN[:10]} ...")
print(f"Chat id is {settings.CHAT_ID}")
print(f"panel url is {settings.PANEL_URL}")
print(f"panel username is {settings.PANEL_USERNAME}")
print(f"panel password is {settings.PANEL_PASSWORD}")
print(f"logger level {settings.LOG_LEVEL}")

